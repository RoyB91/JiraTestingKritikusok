It is a well structured test automation project with using Page Object Model, Data Driven Testing, Docker and Jenkins.

Environment variables:

    UserName;
    PASSWORD;
    CUSTOM_DRIVER;
    BASE_URL
    WAIT_TimeUNIT;
    WINDOWS_SIZE;
